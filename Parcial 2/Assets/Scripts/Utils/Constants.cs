using UnityEngine;

namespace Utils.Constants
{
    /// <summary>
    /// Stores certain variables which are constant throughout the project (for example, that -1 is an invalid position)
    /// </summary>
    public static class Constants
    {
        public static Vector2Int InvalidPosition = new Vector2Int(-1, -1);
    }
}
